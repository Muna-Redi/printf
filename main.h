#ifndef MAIN_H

#define MAIN_H

#include <stdarg.h>

#include <stdlib.h>

#include <string.h>

int _putchar(char c);
int _printf(const char *format, ...);
char *tostr(int num);
char *rev_str(char *);
char *c_base(int num, int base);

#endif
